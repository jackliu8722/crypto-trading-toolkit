# Rust build artifacts
/target
**/*.rs.bk

# Credentials and keys — never commit (AGENTS.md §5)
.env
.env.*
!.env.example
*.pem
*.key
/secrets/
/keys/

# Local runtime data (journals, snapshots, recordings)
*.log
/data/
